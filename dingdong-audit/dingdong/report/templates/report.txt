
### `dingdong/report/templates/report.html`

```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>叮咚猫AI活力审计报告</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #333; }
        .summary { display: flex; gap: 20px; margin: 20px 0; flex-wrap: wrap; }
        .card { border: 1px solid #ddd; border-radius: 5px; padding: 15px; width: 150px; text-align: center; }
        .card.zombie { background-color: #f0f0f0; }
        .card.边缘 { background-color: #e6f3ff; }
        .card.卡顿 { background-color: #ffe6cc; }
        .card.核心 { background-color: #e6ffe6; }
        .card.正常 { background-color: #ffffff; }
        table { border-collapse: collapse; width: 100%; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .zombie-row { background-color: #f0f0f0; }
        .边缘-row { background-color: #e6f3ff; }
        .卡顿-row { background-color: #ffe6cc; }
        .核心-row { background-color: #e6ffe6; }
        .正常-row { background-color: #ffffff; }
        .footer { margin-top: 30px; font-size: 0.9em; color: #777; }
    </style>
</head>
<body>
    <h1>叮咚猫AI活力审计报告</h1>
    <p>生成时间: {{ generated_at }}</p>
    
    <div class="summary">
        <div class="card 正常">
            <h3>总AI数</h3>
            <p>{{ total_ai }}</p>
        </div>
        {% for cls, count in classification_counts.items() %}
        <div class="card {{ cls }}">
            <h3>{{ cls }}AI</h3>
            <p>{{ count }} ({{ "%.1f"|format(count/total_ai*100 if total_ai else 0) }}%)</p>
        </div>
        {% endfor %}
    </div>
    
    <h2>AI分类详情</h2>
    <table>
        <thead>
            <tr>
                <th>AI名称</th>
                <th>分类</th>
                <th>调用次数</th>
                <th>平均响应时间(秒)</th>
                <th>错误率</th>
                <th>部门数</th>
                <th>最后调用时间</th>
            </tr>
        </thead>
        <tbody>
            {% for ai, data in classifications.items() %}
            {% set stats = data.stats %}
            <tr class="{{ data.classification }}-row">
                <td>{{ ai }}</td>
                <td>{{ data.classification }}</td>
                <td>{{ stats.total_calls }}</td>
                <td>{{ "%.2f"|format(stats.avg_response_time) if stats.avg_response_time is not none else 'N/A' }}</td>
                <td>{{ "%.1f"|format(stats.error_rate*100) if stats.error_rate is not none else 'N/A' }}%</td>
                <td>{{ stats.departments|length }}</td>
                <td>{{ stats.last_call.strftime('%Y-%m-%d %H:%M:%S') if stats.last_call else 'N/A' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        <p>报告附带数据溯源包：请在同级目录下查找对应名称的“_溯源包”文件夹。</p>
    </div>
</body>
</html>